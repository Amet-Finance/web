import Image from "next/image";

export default function AbstractHandler({id}: { id: string }) {

    switch (id) {
        case "MainPage": {
            return <>

            </>
        }
    }
}
