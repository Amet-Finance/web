import {GeneralContainer} from "@/components/utils/container";

export default function AddressId({address}: { address: string }) {
    return (
        <GeneralContainer>
            <div className='flex flex-col justify-center items-center'>
                <span className='text-7xl animate-pulse font-semibold'>We are building this page!</span>
            </div>
        </GeneralContainer>
    )
}
